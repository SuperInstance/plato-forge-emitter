[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-forge-emitter"
version = "0.1.0"
description = "Training artifact emission — auto-versioning, quality gates, commit formatting"
license = {text = "MIT"}
requires-python = ">=3.8"
